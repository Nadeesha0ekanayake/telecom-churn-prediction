# Curated dependencies for Path a (VS Code local + local MLflow).
# Human-readable "what we asked for". Exact resolved versions are frozen
# into requirements-lock.txt after install (reproducibility).
# Install:  pip install -r requirements.txt

# --- Core data handling ---
pandas
numpy
scipy

# --- Classic ML ---
scikit-learn

# --- Gradient-boosted trees (the strong models for tabular churn) ---
xgboost
lightgbm

# --- Class imbalance handling (SMOTE etc.) ---
imbalanced-learn

# --- Experiment tracking & model registry ---
mlflow

# --- Model explainability ---
shap

# --- Visualisation ---
matplotlib
seaborn

# --- Interactive dev (VS Code notebooks / interactive window) ---
# ipykernel only: VS Code's Jupyter extension supplies the notebook UI itself,
# so we skip the heavy `jupyter` / JupyterLab metapackage we won't use.
ipykernel

# --- Hyperparameter search (used later in the model zoo) ---
optuna

# --- Model serving (MLflow REST endpoint: `mlflow models serve`) ---
uvicorn
fastapi
